<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
  <% include ./partials/_header %>
  </header>

  <h3>logged in as: <%= user %></h3>

  <table>
  <% Object.keys(userSpecificURLs).forEach(function(shortURL) { %>
    <tr>
      <td> <%= shortURL %> </td>
      <td> <%= userSpecificURLs[shortURL]["longURL"] %> </td>
      <td> <form method="POST" action="/urls/<%= shortURL %>/delete"> <button> delete </button> </form>
      </td>
    </tr>
  <% }) %>
  </table>


  <a href='/urls/new'> add urls! </a>

</body>
</html>