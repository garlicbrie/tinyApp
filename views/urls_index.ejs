<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
  <% include ./partials/_header %>
  </header>


  <table>
  <% Object.keys(userSpecificURLs).forEach(function(shortURL) { %>
    <tr>
      <td> <%= shortURL %> </td>
      <td> <%= userSpecificURLs[shortURL]["longURL"] %> </td>
      <td> <form method="POST" action="/urls/<%= shortURL %>/delete"> <button> delete </button> </form>
      </td>
      <td> <form method="GET" action="urls/<%= shortURL %>"> <button> edit </button></form></td>
    </tr>
  <% }) %>
  </table>


  <a href='/urls/new'> Create a New Short Link! </a>

</body>
</html>